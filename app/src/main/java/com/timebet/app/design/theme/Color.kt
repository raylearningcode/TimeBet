package com.timebet.app.design.theme

import androidx.compose.ui.graphics.Color

// Core palette — Section 29.2
// Background: #000000
// Elevated surface: #0A0A0A to #121212
// Primary text: #FFFFFF
// Secondary text: #A0A0A0 to #B5B5B5
// Tertiary text: #666666 to #777777
// Borders: #1C1C1C to #262626

val TimeBetBlack = Color(0xFF000000)
val TimeBetSurface = Color(0xFF0A0A0A)
val TimeBetSurfaceElevated = Color(0xFF121212)
val TimeBetSurfaceCard = Color(0xFF161616)

val TimeBetWhite = Color(0xFFFFFFFF)
val TimeBetTextPrimary = Color(0xFFFFFFFF)
val TimeBetTextSecondary = Color(0xFFB5B5B5)
val TimeBetTextTertiary = Color(0xFF777777)

val TimeBetBorder = Color(0xFF1C1C1C)
val TimeBetBorderLight = Color(0xFF262626)

// Semantic accents — restrained
val TimeBetGreen = Color(0xFF4ADE80)
val TimeBetGreenMuted = Color(0xFF22C55E)
val TimeBetRed = Color(0xFFEF4444)
val TimeBetRedMuted = Color(0xFFDC2626)
val TimeBetAmber = Color(0xFFF59E0B)
val TimeBetAmberMuted = Color(0xFFD97706)

// Casino-specific accents (subtle, not neon)
val TimeBetGold = Color(0xFFCA8A04)
val TimeBetGoldLight = Color(0xFFEAB308)
